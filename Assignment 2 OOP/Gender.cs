namespace Common
{
    // Part 3: restrict Gender to only Male or Female.
    // Using an enum makes it impossible to assign any other value at compile time.
    public enum Gender
    {
        M,
        F
    }
}
